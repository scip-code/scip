// Generated by the protocol buffer compiler. DO NOT EDIT!
// NO CHECKED-IN PROTOBUF GENCODE
// source: scip.proto

// Generated files should ignore deprecation warnings
@file:Suppress("DEPRECATION")
package scip;

@kotlin.jvm.JvmName("-initializerelationship")
public inline fun relationship(block: scip.RelationshipKt.Dsl.() -> kotlin.Unit): scip.Scip.Relationship =
  scip.RelationshipKt.Dsl._create(scip.Scip.Relationship.newBuilder()).apply { block() }._build()
/**
 * Protobuf type `scip.Relationship`
 */
public object RelationshipKt {
  @kotlin.OptIn(com.google.protobuf.kotlin.OnlyForUseByGeneratedProtoCode::class)
  @com.google.protobuf.kotlin.ProtoDslMarker
  public class Dsl private constructor(
    private val _builder: scip.Scip.Relationship.Builder
  ) {
    public companion object {
      @kotlin.jvm.JvmSynthetic
    @kotlin.PublishedApi
      internal fun _create(builder: scip.Scip.Relationship.Builder): Dsl = Dsl(builder)
    }

    @kotlin.jvm.JvmSynthetic
  @kotlin.PublishedApi
    internal fun _build(): scip.Scip.Relationship = _builder.build()

    /**
     * `string symbol = 1 [json_name = "symbol"];`
     */
    public var symbol: kotlin.String
      @kotlin.jvm.JvmName("getSymbol")
        get() = _builder.symbol
      @kotlin.jvm.JvmName("setSymbol")
        set(value) {
        _builder.symbol = value
      }
    /**
     * `string symbol = 1 [json_name = "symbol"];`
     */
    public fun clearSymbol() {
      _builder.clearSymbol()
    }

    /**
     * ```
     * When resolving "Find references", this field documents what other symbols
     * should be included together with this symbol. For example, consider the
     * following TypeScript code that defines two symbols `Animal#sound()` and
     * `Dog#sound()`:
     * ```ts
     * interface Animal {
     * ^^^^^^ definition Animal#
     * sound(): string
     * ^^^^^ definition Animal#sound()
     * }
     * class Dog implements Animal {
     * ^^^ definition Dog#, relationships = [{symbol: "Animal#", is_implementation: true}]
     * public sound(): string { return "woof" }
     * ^^^^^ definition Dog#sound(), references_symbols = Animal#sound(), relationships = [{symbol: "Animal#sound()", is_implementation:true, is_reference: true}]
     * }
     * const animal: Animal = new Dog()
     * ^^^^^^ reference Animal#
     * console.log(animal.sound())
     * ^^^^^ reference Animal#sound()
     * ```
     * Doing "Find references" on the symbol `Animal#sound()` should return
     * references to the `Dog#sound()` method as well. Vice-versa, doing "Find
     * references" on the `Dog#sound()` method should include references to the
     * `Animal#sound()` method as well.
     * ```
     *
     * `bool is_reference = 2 [json_name = "isReference"];`
     */
    public var isReference: kotlin.Boolean
      @kotlin.jvm.JvmName("getIsReference")
        get() = _builder.isReference
      @kotlin.jvm.JvmName("setIsReference")
        set(value) {
        _builder.isReference = value
      }
    /**
     * ```
     * When resolving "Find references", this field documents what other symbols
     * should be included together with this symbol. For example, consider the
     * following TypeScript code that defines two symbols `Animal#sound()` and
     * `Dog#sound()`:
     * ```ts
     * interface Animal {
     * ^^^^^^ definition Animal#
     * sound(): string
     * ^^^^^ definition Animal#sound()
     * }
     * class Dog implements Animal {
     * ^^^ definition Dog#, relationships = [{symbol: "Animal#", is_implementation: true}]
     * public sound(): string { return "woof" }
     * ^^^^^ definition Dog#sound(), references_symbols = Animal#sound(), relationships = [{symbol: "Animal#sound()", is_implementation:true, is_reference: true}]
     * }
     * const animal: Animal = new Dog()
     * ^^^^^^ reference Animal#
     * console.log(animal.sound())
     * ^^^^^ reference Animal#sound()
     * ```
     * Doing "Find references" on the symbol `Animal#sound()` should return
     * references to the `Dog#sound()` method as well. Vice-versa, doing "Find
     * references" on the `Dog#sound()` method should include references to the
     * `Animal#sound()` method as well.
     * ```
     *
     * `bool is_reference = 2 [json_name = "isReference"];`
     */
    public fun clearIsReference() {
      _builder.clearIsReference()
    }

    /**
     * ```
     * Similar to `is_reference` but for "Find implementations".
     * It's common for `is_implementation` and `is_reference` to both be true but
     * it's not always the case.
     * In the TypeScript example above, observe that `Dog#` has an
     * `is_implementation` relationship with `"Animal#"` but not `is_reference`.
     * This is because "Find references" on the "Animal#" symbol should not return
     * "Dog#". We only want "Dog#" to return as a result for "Find
     * implementations" on the "Animal#" symbol.
     * ```
     *
     * `bool is_implementation = 3 [json_name = "isImplementation"];`
     */
    public var isImplementation: kotlin.Boolean
      @kotlin.jvm.JvmName("getIsImplementation")
        get() = _builder.isImplementation
      @kotlin.jvm.JvmName("setIsImplementation")
        set(value) {
        _builder.isImplementation = value
      }
    /**
     * ```
     * Similar to `is_reference` but for "Find implementations".
     * It's common for `is_implementation` and `is_reference` to both be true but
     * it's not always the case.
     * In the TypeScript example above, observe that `Dog#` has an
     * `is_implementation` relationship with `"Animal#"` but not `is_reference`.
     * This is because "Find references" on the "Animal#" symbol should not return
     * "Dog#". We only want "Dog#" to return as a result for "Find
     * implementations" on the "Animal#" symbol.
     * ```
     *
     * `bool is_implementation = 3 [json_name = "isImplementation"];`
     */
    public fun clearIsImplementation() {
      _builder.clearIsImplementation()
    }

    /**
     * ```
     * Similar to `references_symbols` but for "Go to type definition".
     * ```
     *
     * `bool is_type_definition = 4 [json_name = "isTypeDefinition"];`
     */
    public var isTypeDefinition: kotlin.Boolean
      @kotlin.jvm.JvmName("getIsTypeDefinition")
        get() = _builder.isTypeDefinition
      @kotlin.jvm.JvmName("setIsTypeDefinition")
        set(value) {
        _builder.isTypeDefinition = value
      }
    /**
     * ```
     * Similar to `references_symbols` but for "Go to type definition".
     * ```
     *
     * `bool is_type_definition = 4 [json_name = "isTypeDefinition"];`
     */
    public fun clearIsTypeDefinition() {
      _builder.clearIsTypeDefinition()
    }

    /**
     * ```
     * Allows overriding the behavior of "Go to definition" and "Find references"
     * for symbols which do not have a definition of their own or could
     * potentially have multiple definitions.
     *
     * For example, in a language with single inheritance and no field overriding,
     * inherited fields can reuse the same symbol as the ancestor which declares
     * the field. In such a situation, is_definition is not needed.
     *
     * On the other hand, in languages with single inheritance and some form
     * of mixins, you can use is_definition to relate the symbol to the
     * matching symbol in ancestor classes, and is_reference to relate the
     * symbol to the matching symbol in mixins.
     * ```
     *
     * `bool is_definition = 5 [json_name = "isDefinition"];`
     */
    public var isDefinition: kotlin.Boolean
      @kotlin.jvm.JvmName("getIsDefinition")
        get() = _builder.isDefinition
      @kotlin.jvm.JvmName("setIsDefinition")
        set(value) {
        _builder.isDefinition = value
      }
    /**
     * ```
     * Allows overriding the behavior of "Go to definition" and "Find references"
     * for symbols which do not have a definition of their own or could
     * potentially have multiple definitions.
     *
     * For example, in a language with single inheritance and no field overriding,
     * inherited fields can reuse the same symbol as the ancestor which declares
     * the field. In such a situation, is_definition is not needed.
     *
     * On the other hand, in languages with single inheritance and some form
     * of mixins, you can use is_definition to relate the symbol to the
     * matching symbol in ancestor classes, and is_reference to relate the
     * symbol to the matching symbol in mixins.
     * ```
     *
     * `bool is_definition = 5 [json_name = "isDefinition"];`
     */
    public fun clearIsDefinition() {
      _builder.clearIsDefinition()
    }
  }
}
@kotlin.jvm.JvmSynthetic
public inline fun scip.Scip.Relationship.copy(block: `scip`.RelationshipKt.Dsl.() -> kotlin.Unit): scip.Scip.Relationship =
  `scip`.RelationshipKt.Dsl._create(this.toBuilder()).apply { block() }._build()

